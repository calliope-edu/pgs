/* Localizable Playground Markup */
/* Prose.strings */

"cc.calliope.miniplaygroundbook.Inputs.IssuingCommands" ="The start function is called as soon as Calliope mini is started. The start function contains all commands that are to be executed once at the start of your application. The instructions in the start function are executed one after the other - but very quickly.

![…](trenner_small)

**Goal:** The command `display.show(text:`hello` )` runs the word `Hello` over the LED display.

Read the lines of code and try to understand each command.

1. Tap `String` to enter an individual text.
2. Use the keyboard and enter a text.
4. Tap Run My Code.

![chapter_3](chapter_3_play@1x)

";